{
  "name": "dsh-desktop",
  "private": true,
  "version": "0.6.7",
  "description": "DeepSeek Harness desktop for macOS (Tauri, portless sidecar bridge)",
  "scripts": {
    "dev": "tauri dev",
    "build:app": "tauri build --no-bundle",
    "build:dmg": "tauri build",
    "build:debug": "tauri build --debug"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}